{
  "name": "tokens",
  "projectType": "library",
  "root": "packages/tokens",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npx style-dictionary build --config sd.config.js",
        "cwd": "packages/tokens"
      }
    }
  }
}
